// write a function that lets you watch 2 movies conscecutively. 
// Array of movie times. Return if its possible to watch 2 movies conscecutively. Movie times need to be exact 
//Objects 

const movie_times = [80, 100, 80, 1, 75]
// [80, 100, 80]
const flight = 160 

function is_possible(movie_times, flight){

    const movieObject = {}
    for(let i = 0; i < (movie_times.length); i ++){
        const currentMovie =  movieArray[i]
        if(movieObject.has(currentMovie) {
            
        } 
    }
}

